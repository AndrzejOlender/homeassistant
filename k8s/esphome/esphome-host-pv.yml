apiVersion: v1
kind: PersistentVolume
metadata:
  name: config-esphome
  namespace: smart-home
spec:
  storageClassName: "config-esphome"
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/opt/docker/esphome/data"
